language: python
script:
- ./build.sh
branches:
  only:
  - master
deploy:
  provider: s3
  access_key_id: "YOUR AWS ACCESS KEY"
  secret_access_key: "YOUR AWS SECRET KEY"
  bucket: "ulauncher.io"
  local_dir: _site
  skip_cleanup: true
